{
  "server": {
    "port": "PORT",
    "host": "HOST"
  },
  "upload": {
    "maxSize": "MAX_FILE_SIZE",
    "tempDir": "UPLOAD_TEMP_DIR"
  },
  "queue": {
    "type": "QUEUE_TYPE",
    "concurrency": "QUEUE_CONCURRENCY",
    "retention": "TASK_RETENTION_HOURS",
    "redis": {
      "host": "REDIS_HOST",
      "port": "REDIS_PORT",
      "db": "REDIS_DB",
      "password": "REDIS_PASSWORD"
    }
  },
  "storage": {
    "type": "STORAGE_TYPE",
    "local": {
      "path": "STORAGE_LOCAL_PATH"
    },
    "s3": {
      "bucket": "S3_BUCKET",
      "region": "S3_REGION",
      "accessKeyId": "S3_ACCESS_KEY_ID",
      "secretAccessKey": "S3_SECRET_ACCESS_KEY"
    }
  },
  "pptist": {
    "defaultVersion": "PPTIST_DEFAULT_VERSION",
    "supportedVersions": "PPTIST_SUPPORTED_VERSIONS"
  },
  "logging": {
    "level": "LOG_LEVEL",
    "format": "LOG_FORMAT"
  },
  "metrics": {
    "enabled": "METRICS_ENABLED",
    "port": "METRICS_PORT"
  },
  "tracing": {
    "enabled": "TRACING_ENABLED",
    "exporter": "TRACING_EXPORTER",
    "endpoint": "TRACING_ENDPOINT"
  },
  "security": {
    "helmet": {
      "enabled": "HELMET_ENABLED"
    },
    "rateLimit": {
      "enabled": "RATE_LIMIT_ENABLED",
      "max": "RATE_LIMIT_MAX",
      "windowMs": "RATE_LIMIT_WINDOW"
    }
  }
}
